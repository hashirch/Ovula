[phases.setup]
nixPkgs = ["python39", "postgresql"]

[phases.install]
cmds = ["cd src/backend && pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd src/backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
